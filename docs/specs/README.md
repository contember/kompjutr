# Accepted specs

Accepted pre-build designs and frozen implementation evidence live here. Once
accepted, a spec is incorporated by the active sprint that links it and changes
only through an explicit superseding correction, not casual drift. Path is the
lifecycle signal; specs carry no status field.
